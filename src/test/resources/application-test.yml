spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:celebe-test.db
    username: sa
    password: sa
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:profile-test-data.sql